# 大月曆預約系統 使用者說明書

版本：1.0.0　|　適用系統：大月曆預約系統（Calendar Booking System）

---

## 目錄

1. [系統簡介](#1-系統簡介)
2. [系統需求與啟動方式](#2-系統需求與啟動方式)
3. [登入與權限](#3-登入與權限)
4. [介面導覽](#4-介面導覽)
5. [月／週／日三種檢視](#5-月週日三種檢視)
6. [新增預約](#6-新增預約)
7. [查看、編輯、刪除預約](#7-查看編輯刪除預約)
8. [複製與貼上預約（Outlook 式）](#8-複製與貼上預約outlook-式)
9. [篩選功能](#9-篩選功能)
10. [匯入 Excel](#10-匯入-excel)
11. [匯出 Excel／PDF](#11-匯出-excelpdf)
12. [批量刪除預約](#12-批量刪除預約)
13. [系統設定（房間／員工／公告／回收站／操作紀錄）](#13-系統設定房間員工公告回收站操作紀錄)
14. [代辦事項](#14-代辦事項)
15. [員工假期](#15-員工假期)
16. [公眾假期](#16-公眾假期)
17. [房間顏色管理](#17-房間顏色管理)
18. [鍵盤快捷鍵](#18-鍵盤快捷鍵)
19. [常見問題](#19-常見問題)

---

## 1. 系統簡介

大月曆預約系統是一套網頁版的**會議室／房間預約管理工具**，提供：

- 月曆、週、日三種檢視方式
- 跨日預約、時間衝突自動偵測
- 預約的複製／貼上、批量刪除
- Excel 匯入／匯出、PDF 匯出
- 代辦事項、員工假期、公眾假期管理
- 房間與員工管理、頂部公告、操作紀錄

---

## 2. 系統需求與啟動方式

### 技術環境

| 項目 | 說明 |
|---|---|
| 後端 | Node.js + Express |
| 資料庫 | PostgreSQL（透過 `DATABASE_URL` 環境變數連線） |
| 前端 | 原生 HTML / CSS / JavaScript（無需建置） |

### 啟動步驟（本機）

1. 安裝依賴套件：

   ```bash
   npm install
   ```

2. 設定資料庫連線（如有資料庫）：

   ```bash
   export DATABASE_URL="postgres://帳號:密碼@主機:5432/資料庫名稱"
   ```

3. 啟動伺服器：

   ```bash
   npm start
   ```

4. 開啟瀏覽器連到：`http://localhost:3000`

> 若未設定 `DATABASE_URL`，系統仍可啟動，但僅能提供頁面與 API 框架，無法存取資料。

### 雲端部署（Render）

專案內附 `render.yaml`，部署時需設定環境變數 `DATABASE_URL`。系統首次啟動會自動建立資料表並填入預設資料（詳見下一節）。

---

## 3. 登入與權限

本系統**一般瀏覽與新增預約不需要登入**，只有以下管理功能需要管理密碼：

| 功能 | 需要密碼 |
|---|---|
| 瀏覽月曆、新增／編輯／刪除單筆預約、匯入／匯出 | ❌ 不需要 |
| 批量刪除預約 | ✅ 需要（管理密碼） |
| 系統設定（房間／員工／公告／回收站） | ✅ 需要（管理密碼） |

### 預設管理員帳號

- **帳號**：`admin`
- **預設密碼**：`123456`

> 點擊「批量刪除」垃圾桶按鈕或「系統設定」齒輪按鈕時，會彈出密碼輸入視窗。建議部署後立即變更預設密碼。

---

## 4. 介面導覽

```
┌──────────────────────────────────────────────────────────┐
│  ◀  2026  January  ▶  [本月]  [檢視:月] [員工篩選] [房間篩選]  │ ← 頂部工具列
│  [匯入Excel] [i] [匯出] [批量刪除] [設定] [代辦事項]          │
│  公告橫條（可於設定中修改或關閉）                          │ ← 公告欄
├───────────┬──────────────────────────────────────────────┤
│ 左側欄      │                                              │
│ · 房間色塊  │            月曆／時間軸主視圖                  │
│ · 迷你月曆  │                                              │
├───────────┴──────────────────────────────────────────────┤
```

### 頂部工具列功能一覽

| 按鈕／元件 | 功能 |
|---|---|
| ◀ ▶ | 上一頁／下一頁（月／週／日依目前檢視切換） |
| 月份標題 | 顯示目前年月份 |
| 本月 | 一鍵回到本月（並切回月檢視） |
| 檢視下拉選單 | 切換 Month／Week／Day 三種檢視 |
| 員工篩選下拉 | 依員工過濾顯示 |
| 房間篩選（多選） | 可勾選多個房間過濾顯示 |
| 匯入 Excel 按鈕（📥） | 匯入預約／代辦／員工假期 |
| 「i」小按鈕 | 顯示匯入格式規範與本次匯入結果明細 |
| 匯出按鈕（⬇） | 匯出 Excel 或 PDF |
| 批量刪除（🗑） | 需管理密碼，可整月／整日清除預約 |
| 設定（⚙） | 需管理密碼，管理房間、員工、公告、回收站、操作紀錄 |
| 代辦事項（☑） | 新增／管理代辦事項與員工假期 |

### 左側欄

- **房間色塊**：列出所有房間，點擊色塊名稱可**多選篩選**；點擊色塊前方的**圓點**可開啟「房間顏色選擇器」。
- **迷你月曆**：點擊日期可快速跳轉；點「‹ ›」切換月份。

---

## 5. 月／週／日三種檢視

### 月檢視（Month）

- 以月曆方格顯示整月。
- **日期上方標記**：今天會反白標示；公眾假期日期會顯示**假期名稱**。
- 每個日期下方會依序顯示：**預約**（彩色，以房間顏色區分）、**代辦事項**（黃色）、**員工假期**（綠色）。
- 跨日預約在結束日會加上 `[跨日]` 標記。
- **點擊日期** → 開啟該日的新增預約視窗。
- **點擊預約標籤** → 顯示預約詳細資料。

### 週檢視（Week）

- 以時間軸（每 30 分鐘一格）顯示一週七天。
- 上方顯示星期標題，左側顯示時間刻度，「全天」列顯示全日活動。
- 點擊時間軸空白處可於該時段新增預約。

### 日檢視（Day）

- 僅顯示單一日的時間軸，操作方式與週檢視相同。
- 頁首「匯出範圍」會多出「當日」選項（見第 11 節）。

---

## 6. 新增預約

在月檢視**點擊任一日期**，或在週／日檢視**點擊時間軸空白處**，即可開啟「新增預約」視窗。

### 表單欄位

| 欄位 | 說明 |
|---|---|
| 活動名稱 | 必填，例如「AI 訓練」 |
| 員工姓名 | 必填，下拉選單；選「其他」可輸入自訂姓名 |
| 選擇房間 | 必填，下拉選單；選「其他」可輸入自訂房間（會自動建立該房間並配色） |
| 開始／結束時間 | 每 30 分鐘一級；選擇開始時間後會自動帶入「＋1 小時」的結束時間 |
| 快速時段 | 一鍵填入：全日 09:00–18:00／上午 09:00–13:00／下午 14:00–18:00 |
| 備註 | 選填，例如會議 Zoom 連結 |

### 規則與自動判斷

- **所有欄位皆為必填**（備註除外），格式錯誤時會出現提示。
- **跨日預約**：若「結束時間」早於或等於「開始時間」（例如 22:00–01:00），系統自動視為隔日結束，無需另外選日期。
- **衝突偵測**：同房間同一時段已被他人預約時，系統會跳出「該時段已被 ○○○ 預約」並拒絕建立。
- **輸入清洗**：多餘空白、換行、特殊符號會被自動過濾。

### 確認送出

點「**確認預約**」按鈕即可。送出成功後月曆會即時更新。

---

## 7. 查看、編輯、刪除預約

點擊月曆上的任一預約標籤，會彈出**預約詳情視窗**，右上角提供四個功能：

| 圖示 | 功能 |
|---|---|
| 🗑 刪除 | 刪除該筆預約（會再次確認） |
| 📋 複製 | 複製該預約（Outlook 式，見下節） |
| ✏️ 編輯 | 開啟編輯視窗，可修改所有欄位 |
| ✕ 關閉 | 關閉視窗 |

### 查看備註

若該預約有備註，詳情視窗會顯示「**查看備註**」按鈕；點開後可閱讀備註全文，並按 **Copy** 一鍵複製到剪貼簿。

---

## 8. 複製與貼上預約（Outlook 式）

> 僅改變**日期**，活動名稱、時間、房間、負責人皆沿用原預約，適合排定每週例行會議。

### 複製

1. 開啟任一預約的詳情視窗。
2. 點「📋 複製」按鈕，**或**在詳情視窗開啟時按快捷鍵 `Ctrl / ⌘ + C`。

### 貼上

3. 複製後，畫面下方會出現**貼上浮動列**，顯示「已複製：○○○｜房間 時段」。
4. 在浮動列選擇**目標日期**（預設為目前選取的日期）。
5. 按「**貼上**」→ 系統會開啟帶有預填資料的新增預約視窗，確認後送出。
   - **或**直接按快捷鍵 `Ctrl / ⌘ + V`，以目前選取日期作為目標。
6. 按「**清除**」可取消複製。

---

## 9. 篩選功能

### 依員工篩選

在頂部「員工篩選」下拉選單選擇特定員工，月曆／時間軸只會顯示該員工的預約、代辦與假期。

### 依房間篩選（多選）

- 點擊頂部「房間篩選」按鈕，可勾選多個房間；勾選「全部」可一次顯示所有房間。
- 亦可使用左側欄的房間色塊，點擊名稱切換該房間的顯示／隱藏。

> 篩選僅影響**顯示**，不影響資料本身；匯出時也會套用目前的篩選條件。

---

## 10. 匯入 Excel

支援**多 Sheet** 的 Excel 檔案（.xlsx／.xls），系統會自動偵測每個 Sheet 的欄位格式並分別匯入。

### 操作方式

1. 點擊頂部「📥 匯入 Excel」按鈕，選擇檔案。
2. 匯入完成後會顯示結果：成功幾條、跳過幾條、自動新增的房間／員工數量。
3. 點擊匯入按鈕旁的「**i**」小按鈕，可再次查看**格式規範**與**本次跳過的全部異常明細**。

### 各 Sheet 格式規範

**Sheet 1「預約」** — 欄位：`日期`、`活動名稱`、`預約員工`、`房間`、`開始時間`、`結束時間`（備註選填）

- 跨日預約：結束時間早於開始時間時，自動視為隔日結束。
- 亦支援舊版格式：`日期（含星期）`、`活動名`、`時段（全日／am／pm）`、`房間`、`負責人`。

**Sheet 2「待辦事項」** — 欄位：`標題`、`開始日期`、`結束日期`、`開始時間`、`結束時間`、`房間`、`負責人`、`全日`。

**Sheet 3「公眾假期」** — 由系統自動抓取，**無需匯入**。

**Sheet 4「員工假期」** — 欄位：`員工姓名`、`開始日期`、`結束日期`（同日＝單日）、`假期類型`（選填）。

### 格式範例

```
日期         活動名稱   預約員工   房間      開始時間  結束時間
2026-01-15  AI 訓練    Pius     VIP Room   09:00    18:00
```

- 時間格式：`09:00`、`23:30`
- 日期格式：`2026-01-15`（舊格式亦支援 `1/03/2026（Fri）`）

### 匯入規則

- 同房間同時間衝突的預約會被**自動跳過**並記錄原因。
- 檔案中出現的新房間、新員工會**自動建立**（房間會自動配顏色）。
- 重複的代辦事項與員工假期會被略過。
- 若全部資料都被跳過，通常是**欄位名稱與規範不符**，請點「i」按鈕查看錯誤原因。

---

## 11. 匯出 Excel／PDF

點擊頂部「⬇ 匯出」按鈕，選擇範圍與格式。

### 匯出範圍

| 範圍 | 顯示條件 |
|---|---|
| 當前顯示月份 | 月檢視時可用 |
| 當週 | 週檢視時可用 |
| 當日 | 日檢視時可用 |
| 全年所有預約 | 任何檢視皆可用 |

> 範圍與目前檢視不符時，系統會拒絕並提示重新選擇。

### Excel 格式

匯出的 .xlsx 包含 **4 個 Sheet**：

| Sheet | 內容 |
|---|---|
| 預約 | 日期、活動名稱、預約員工、房間、開始時間、結束時間、備註 |
| 待辦事項 | 標題、開始／結束日期與時間、房間、負責人、全日 |
| 公眾假期 | 日期、名稱、英文名稱 |
| 員工假期 | 員工姓名、開始／結束日期、假期類型 |

### PDF 格式

匯出「完整月曆畫面」PDF，保留月曆版面與預約色塊，適合列印或張貼。

> 匯出會套用目前選取的員工／房間篩選條件。

---

## 12. 批量刪除預約

> ⚠️ **危險操作**：一次刪除整月或整日的所有預約，**無法復原**，使用前請確認。

### 操作方式

1. 點擊頂部「🗑 批量刪除」按鈕。
2. 輸入**管理密碼**（預設 `123456`）驗證身份。
3. 驗證通過後選擇：
   - **清除本月所有預約**（需在月檢視下才可操作）
   - **清除當日所有預約**（需在週／日檢視下才可操作）
4. 系統會再次跳出確認，輸入確認後才執行刪除。

---

## 13. 系統設定（房間／員工／公告／回收站／操作紀錄）

點擊頂部「⚙ 設定」並輸入管理密碼後開啟。內容分為五個區塊：

### 13.1 房間列表

- 在「輸入新房間名稱」輸入名稱後按**新增**。
- 每個房間可設定**縮寫**（用於月曆上節省空間的顯示），輸入後移開游標即自動儲存。
- 按 ✕ 可刪除房間；**尚有預約的房間無法刪除**，需先刪除該房間的預約。刪除的房間會進入「回收站」。

### 13.2 員工列表

- 輸入姓名後按**新增**；按 ✕ 刪除員工。
- 刪除員工不會影響既有的預約資料。

### 13.3 頂部公告設定

- 輸入公告文字後按「**儲存公告**」。
- 公告會顯示在月曆頂部的橫條。
- **留空白**即關閉公告。

### 13.4 房間回收站

- 顯示所有被刪除的房間，提供「**恢復**」與「**永久刪除**」。
- 「**一鍵永久清空**」可一次清除回收站所有房間（無法復原）。
- 點擊「ⓘ」可查看回收站說明。

### 13.5 操作紀錄

- 顯示最近的操作紀錄（新增、更新、刪除預約／房間／員工等），每頁 30 筆。
- 按「**載入更多**」可查看更早的紀錄。

---

## 14. 代辦事項

點擊頂部「☑ 代辦事項」按鈕開啟管理視窗（此功能**不需管理密碼**）。

### 新增代辦事項

| 欄位 | 說明 |
|---|---|
| 代辦事項名稱 | 必填 |
| 開始／結束日期 | 必填 |
| 開始／結束時間 | 選填，可留「不指定」 |
| 房間、負責人 | 選填 |
| 全日 | 勾選後不指定時間 |
| 重複 | 可勾選多個星期（一～日），系統會在日期範圍內每個勾選的星期自動建立一筆 |

### 管理代辦

- 代辦清單會以「相同名稱＋時間＋房間＋負責人」**自動分組**，重複建立的項目會顯示「共 N 條」及星期規律。
- 點擊代辦事項可查看詳情（可**編輯**或**刪除**）。
- 按 ✕ 可**整批刪除**同一組的所有代辦事項。
- 月曆上以**黃色**標籤顯示。

---

## 15. 員工假期

在「代辦事項」視窗內切換到「**員工假期**」頁籤。

### 新增員工假期

| 欄位 | 說明 |
|---|---|
| 員工 | 從下拉選單選擇 |
| 假期類型 | 選填，例如年假、病假、事假 |
| 開始／結束日期 | 結束日期與開始相同＝單日；不同＝跨日假期 |

- 新增後會顯示在下方清單，可個別刪除。
- 月曆上以**綠色**標籤顯示，跨日假期會在期間每一天顯示，續日會加上 `[跨日]` 標記。
- 點擊月曆上的假期標籤可查看詳情。

---

## 16. 公眾假期

- 系統依「香港（HK）」資料，透過 Nager.Date 服務**自動抓取**每年公眾假期並存入資料庫快取。
- 若當年度尚未抓取，系統會在開啟頁面時自動下載；網路不可用時會退回使用已快取的資料。
- 公眾假期日期在月曆上會以**紅色標記**並顯示假期名稱。
- 匯出的 Excel「公眾假期」Sheet 也會包含這些資料。

---

## 17. 房間顏色管理

- 每個房間在月曆上都有一個專屬顏色（深色調、白色文字，適合閱讀）。
- 在**左側欄**點擊房間色塊前方的**圓點**，即可開啟**顏色選擇器**：
  - 拖曳上方漸層區選擇明暗／飽和度。
  - 拖曳色相條選擇色相。
  - 可輸入六碼 HEX 色碼（不含 `#`）。
  - 下方有**快速色票**可直接點選。
- 按「確認」後顏色立即生效，並存入後端與你的瀏覽器（localStorage），下次開啟仍會保留。
- 新建立的房間會自動從系統色票分配一個柔和顏色。

---

## 18. 鍵盤快捷鍵

| 快捷鍵 | 功能 | 使用情境 |
|---|---|---|
| `Ctrl / ⌘ + C` | 複製目前查看的預約 | 預約詳情視窗開啟時 |
| `Ctrl / ⌘ + V` | 貼上複製的預約到目前選取的日期 | 已複製預約後 |
| `Enter` | 確認（密碼輸入視窗） | 管理密碼彈窗 |
| `Esc` | 取消（密碼輸入視窗） | 管理密碼彈窗 |

> 當焦點停留在輸入框、下拉選單、文字區內時，快捷鍵不會觸發，避免干擾輸入。

---

## 19. 常見問題

**Q1：登入頁面在哪裡？**
A：本系統沒有獨立登入頁。一般功能不需登入；「批量刪除」與「系統設定」在點擊時會彈出密碼輸入視窗。

**Q2：忘了管理密碼怎麼辦？**
A：預設密碼為 `123456`。可請系統管理者直接修改資料庫 `users` 表中 `admin` 的密碼（密碼以 bcrypt 加密）。

**Q3：匯入 Excel 全部被跳過？**
A：絕大多數是欄位名稱不符。點擊匯入按鈕旁的「i」小按鈕，查看格式規範與跳過明細，確認表頭名稱完全一致（例如「活動名稱」不能寫成「活動名」以外的名稱、時間格式必須是 `09:00`）。

**Q4：為什麼無法刪除某個房間？**
A：該房間尚有未被刪除的預約。請先刪除或確認該房間所有預約後再刪除房間。

**Q5：跨日預約怎麼建立？**
A：直接讓「結束時間」早於「開始時間」（例如開始 22:00、結束 01:00），系統會自動視為隔日結束。

**Q6：公眾假期沒有顯示？**
A：系統會自動抓取香港公眾假期並快取。若遇網路問題可能暫時無法取得，可稍後重新整理頁面。

**Q7：匯出 PDF 內容與月曆畫面不同？**
A：請確認目前的檢視為「月」檢視，並依第 11 節選擇相符的匯出範圍。

---

*本說明書對應原始碼版本：大月曆預約系統 v1.0.0。功能細節以實際系統為準。*
